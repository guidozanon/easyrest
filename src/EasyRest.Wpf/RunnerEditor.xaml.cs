using System.Windows.Controls;

namespace EasyRest;

/// <summary>Placeholder: el runner (usuarios virtuales, corridas y comparación) vive en el head Avalonia.</summary>
public partial class RunnerEditor : UserControl
{
    public RunnerEditor() => InitializeComponent();
}
