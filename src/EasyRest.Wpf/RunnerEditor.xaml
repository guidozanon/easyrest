<UserControl x:Class="EasyRest.RunnerEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Foreground="{StaticResource TextBrush}">
    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="480">
        <TextBlock Text="Runner" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center"
                   Foreground="{StaticResource TextBrush}" Margin="0,0,0,8"/>
        <TextBlock TextWrapping="Wrap" TextAlignment="Center" FontSize="13"
                   Foreground="{StaticResource SubtextBrush}"
                   Text="El runner rediseñado (usuarios virtuales, ramp-up, corridas guardadas y comparación) está disponible en la versión multiplataforma (Avalonia). El head WPF quedó sin runner."/>
    </StackPanel>
</UserControl>
